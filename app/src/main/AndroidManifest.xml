<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AerolyfQuizApp"
        tools:targetApi="31">
        <activity
            android:name=".activities.AerolyfAppLoginScreen"
            android:exported="false"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar">
        </activity>
        <activity
            android:name=".activities.AerolyfRegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar">
        </activity>
        <activity
            android:name=".activities.AerolyfQuizQuestionActivity"
            android:exported="false"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar" />
        <activity
            android:name=".activities.AerolyfQuestionTopicCategoryActivity"
            android:exported="false"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar" />
        <activity
            android:name=".activities.ScoreActivity"
            android:exported="false"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AerolyfQuizApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>